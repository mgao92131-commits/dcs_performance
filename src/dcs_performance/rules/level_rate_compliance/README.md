# 酯化液位变化速率考核

本规则先对 `LICA-012019/PID1/PV.CV` 做60秒尾随滑动平均，再计算最近2小时液位变化速率。

判定标准：

- 速率小于 `-0.14 液位/小时`，并连续达到2小时，生成下降事件；
- 速率大于 `+0.14 液位/小时`，并连续达到2小时，生成上升事件；
- 速率等于边界值不算超限；
- 连续时长按时间戳计算，达到7200秒即可确认。

绝对液位71～73由 `analog_limit_exceedance` 负责，本规则只负责速率。
