# 示例考核规则

这是第一阶段用于验证规则接口、规则配置、Loader、Runner 和 Engine 链路的空规则。

- 规则 ID：`example_rule`
- 默认状态：启用
- 实际考核窗口：班次开始后 20 分钟至班次结束
- 行为：不读取 DCS 数据，始终返回空的 `list[AssessmentEvent]`

这里的评分字段只是配置结构示例，第一阶段不会据此生成评分结果。
